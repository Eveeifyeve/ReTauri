{
  "name": "Retauri",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "update": "npm install && cd src-tauri && cargo update",
    "dev": "npm run tauri dev",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  }
}
